const home = () => "/";

const signIn = () => "/auth/sign-in";

const dashboard = () => "/dashboard";

const createSubmission = () => "/dashboard/create-submission";

const trackSubmission = () => "/track";

const routes = {
  home,
  signIn,
  dashboard,
  createSubmission,
  trackSubmission,
};

export default routes;
